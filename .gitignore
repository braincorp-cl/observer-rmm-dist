*.retry
*.pyc
__pycache__/
.vault_pass
*.log

# Inventario de entorno real (IP del lab + admin_url ofuscado) — usar staging.yml.example
inventory/staging.yml
inventory/linux-endpoints.yml
.molecule/
.cache/

# Frontend (Feature 003)
web/node_modules/
web/dist/
web/.env.production
web/.env.development

# Coverage artifacts (regenerados por pytest).
# El paquete Django es `observerrmm/` desde el rename de F007; estas reglas decían
# `api/observer/` y llevaban desde entonces sin ignorar nada.
api/observerrmm/coverage.xml
api/observerrmm/.coverage
api/observerrmm/htmlcov/
api/observerrmm/.pytest_cache/

# Config de NATS: la GENERA el servidor con `manage.py reload_nats`, y el deploy
# la excluye del synchronize en los dos caminos (deploy-api.yml y el rol). Correr
# la suite en local la deja escrita acá con credenciales de juguete; se coló al
# índice en `0c0345c` y de ahí sale esta regla.
api/observerrmm/nats-rmm.conf

group_vars/*/vault*.yml
inventory/produccion.yml

# ─────────────────────────────────────────────────────────────────────────────
# Este repo es el PRODUCTO: un `git clone` debe traer sólo lo que se instala con
# el playbook, y nada más. Ni specs, ni salidas de test, ni configuración de
# editores o herramientas de nadie.
# ─────────────────────────────────────────────────────────────────────────────

# Caché de Ansible (roles/modules/collections locales). Hoy el directorio existe
# vacío, así que no se subía por una razón frágil: git no versiona directorios
# vacíos. En cuanto Ansible escriba algo dentro, un `git add -A` se lo llevaría.
.ansible/

# Editores e IDEs
.vscode/
.idea/
*.swp
*~
.DS_Store

# Herramientas de asistencia (no son componentes del producto)
.claude/
.agents/
CLAUDE.md
AGENTS.md

# Framework Reversa: vive en el hub `observer-rmm`, que es el repo de specs.
# Acá no entra nada, ni siquiera de paso.
.reversa/
_reversa_sdd/
_reversa_forward/

# Salidas de pruebas y diagnósticos: se regeneran, no se versionan.
smoke-results/
*.orig
*.rej

# Virtualenvs. Hoy `.venv-002/` (548 MB) NO lo ve git, pero por un accidente
# parecido al de `.ansible/`: `uv` y el `venv` moderno dejan un `.gitignore` con
# `*` DENTRO del propio venv. Un venv creado con una herramienta que no lo haga
# quedaría a un `git add -A` de colarse.
.venv*/
venv/
env/
ENV/
