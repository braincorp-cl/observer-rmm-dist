# Toda la cadena de pruebas va pinneada a versiones exactas, y a proposito.
#
# Sin pin, el gate se pone rojo solo el dia que un paquete de terceros publica
# una version nueva, sin que nadie haya tocado el codigo. Ya paso dos veces:
#
#   black             cambia de criterio de formato -> `--check` falla
#   pytest-django     la 4.13.0 (2026-08-05) dejo caer Django 4.2 LTS: su fixture
#                     _django_db_helper lee SimpleTestCase._pre_setup_ran_eagerly,
#                     atributo que solo existe desde Django 5.2. Reventaron 232
#                     tests con marca django_db en un commit que solo tocaba un
#                     .ps1. El paquete NO declara restriccion sobre Django, asi
#                     que pip lo instala igual y falla recien en ejecucion.
#
# ⚠️ pytest-django tiene ademas un TECHO DURO: no puede pasar de 4.12.x mientras
# los servidores corran Django 4.2 LTS. Los demas pines son de reproducibilidad
# y se pueden subir cuando se quiera, con la CI como testigo.
#
# Versiones tomadas de la ultima corrida integramente verde (2026-08-05, run
# 30976847240) y verificadas: las 12 tienen rueda cp311 manylinux, que es lo que
# corre el runner (Python 3.11.8). Dependabot esta DESACTIVADO en este repo
# (medido 2026-08-06 por API), asi que nadie va a avisar de los bumps: revisar
# a mano cada cierto tiempo, o el pin se vuelve deuda silenciosa.

coverage==7.15.3
model_bakery==1.23.5
black==26.5.1
pytest==9.1.1
pytest-django==4.12.0
pytest-xdist==3.8.0
pytest-cov==7.1.0
pytest-randomly==4.1.0
refurb==2.3.1
flake8==7.3.0
daphne==4.2.3
time-machine==3.3.1
